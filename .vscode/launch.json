{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "rdbg",
      "name": "Rails server (todo_app, bundle exec rdbg)",
      "request": "launch",
      "cwd": "${workspaceFolder}/todo_app",
      "rdbgPath": "bundle exec rdbg",
      "command": "bin/rails",
      "script": "server",
      "useBundler": true
    }
  ]
}